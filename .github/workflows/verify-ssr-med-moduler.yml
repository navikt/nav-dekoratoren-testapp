name: Bekreft SSR med moduler
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Deploy dev"]
    types: [completed]
jobs:
  bekreft:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/verify.yml
    with:
      path: /ssr-med-moduler
      spec: tests/ssr-med-moduler.spec.ts
    secrets: inherit
