name: Verify direct CSR
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Deploy dev"]
    types: [completed]
jobs:
  verify:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    uses: ./.github/workflows/verify.yml
    with:
      path: /csr-direkte
      spec: tests/csr-direkte.spec.ts
    secrets: inherit
